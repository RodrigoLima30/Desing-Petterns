export class Inventario {
    usarItem(item: string) {
        console.log(`Item ${item} usado.`);
    }

    trocarArma(arma: string) {
        console.log(`Arma trocada para ${arma}.`);
    }
}